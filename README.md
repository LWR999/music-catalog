# music-catalog (v0)

Read-only indexer for NAS-based libraries. Manual commands only.

## Commands
- `mc migrate` — create/upgrade SQLite schema
- `mc inventory fast --config /home/dl/development/.config/music-catalog/dev.yaml`
- `mc inventory deep --config /home/dl/development/.config/music-catalog/dev.yaml`

## Config
Copy `config-example.yaml` to `/home/dl/development/.config/music-catalog/dev.yaml` and edit paths.
